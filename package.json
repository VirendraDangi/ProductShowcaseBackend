{
  "name": "product-showcase-backend",
  "version": "1.0.0",
  "description": "Fake product API using json-server",
  "scripts": {
   
    "start": "json-server --watch db.json --port $PORT --static ."
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
